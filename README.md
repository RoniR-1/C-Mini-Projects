1. A library of various known methodes and a linked list.
2. A copy of printf, %-,%0,%.,%#,%+,% (space) FLAGS aren't implemented yet.
3. Get the next line of a file.
4. Draws an isometric projection of an fdf file (formatted text file containing a grid of integers that represent 3D elevation coordinates (x, y, z)). You can additionally rotate it along the 3 axis, X,Y,Z. This uses the MiniLibX library for the rendering.
5. An implementation of the classic [Dining Philosophers Problem](https://en.wikipedia.org/wiki/Dining_philosophers_problem) using threads (pthread) and mutexes (pthread_mutex_t) in C. The program simulates N philosophers sitting around a table with shared forks, managing concurrent execution, synchronized logging, and starvation prevention through thread-safe monitoring and resource hierarchy locks.
