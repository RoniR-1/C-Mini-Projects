1. A library of various known methodes and a linked list.
2. A copy of printf, %-,%0,%.,%#,%+,% (space) FLAGS aren't implemented yet.
3. Get the next line of a file.
4. Draws an isometric projection of an fdf file (formatted text file containing a grid of integers that represent 3D elevation coordinates (x, y, z)). You can additionally rotate it along the 3 axis, X,Y,Z. This uses the MiniLibX library for the rendering.
